export default class KintoneNoteRepositoryConfig {
    cybozuHost: string;
    appId: number;
    xCybozuAuthorization: string;
    // 'X-Cybozu-Authorization': encodeBase64(authInfo.username + ':' + authInfo.password)
}